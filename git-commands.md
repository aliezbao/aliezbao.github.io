# GitHub 博客更新指令大全

## 📝 基础工作流

```bash
# 1. 进入博客文件夹
cd miniblog

# 2. 查看文件状态
git status

# 3. 添加所有修改的文件
git add .

# 4. 提交更改（填写说明信息）
git commit -m "更新说明信息"

# 5. 推送到 GitHub
git push origin main
```

---

## 🎯 常用指令速查表

### 查看状态

```bash
# 查看修改了什么文件
git status

# 查看具体改了什么内容
git diff

# 查看提交历史
git log

# 查看最近提交（简洁版）
git log --oneline
```

### 添加文件

```bash
# 添加所有修改
git add .

# 添加特定文件
git add 文件名

# 添加所有新文件
git add -A
```

### 提交

```bash
# 提交并写说明
git commit -m "提交说明"

# 修改上一次提交（还没推送时）
git commit --amend -m "新的说明"
```

### 推送

```bash
# 推送到 GitHub
git push origin main

# 强制推送（覆盖远程，谨慎使用）
git push -f origin main

# 推送并设置默认分支
git push -u origin main
```

### 拉取

```bash
# 拉取远程最新内容
git pull origin main

# 只查看更新，不合并
git fetch origin main
```

### 撤销修改

```bash
# 撤销未添加的修改
git checkout -- 文件名

# 撤销已添加的文件
git reset HEAD 文件名

# 回到上一次提交（删除所有修改）
git reset --hard HEAD

# 撤销上一次提交（但保留文件）
git reset --soft HEAD~1
```

---

## 🚀 最常用的三条指令

```bash
# 每次更新博客后执行这三条

# 1. 添加所有修改
git add .

# 2. 提交（写上你的修改说明）
git commit -m "更新博客内容"

# 3. 推送到 GitHub
git push
```

---

## 💡 快速别名设置

```bash
# 设置别名（一次性）
git config --global alias.quick '!git add . && git commit -m'

# 以后就可以这样用
git quick "更新博客"
git push
```

---

## 📋 实际场景示例

### 场景1：更新博客文章

```bash
cd miniblog
git add .
git commit -m "新增：Django 学习笔记"
git push
```

### 场景2：修复 CSS 样式

```bash
git add .
git commit -m "修复：首页导航栏样式"
git push
```

### 场景3：多个小改动

```bash
git add .
git commit -m "优化：
- 更新首页文案
- 修复响应式布局
- 添加新分类"
git push
```

### 场景4：撤销刚才的修改

```bash
# 还没 push 的情况
git reset --hard HEAD

# 已经 push 的情况（需要强制覆盖）
git reset --hard HEAD~1
git push -f
```

---

## ⚙️ 查看远程仓库信息

```bash
# 查看远程仓库地址
git remote -v

# 如果需要修改远程地址
git remote set-url origin https://github.com/aliezbao/miniblog.git
```

---

## 🔄 完整工作流示例

```bash
# 早上开始工作
cd miniblog
git pull origin main        # 同步最新内容

# 工作一段时间，修改了文件...

# 准备提交
git status                  # 查看修改了什么
git diff                    # 查看具体改了什么

# 提交
git add .
git commit -m "今天的工作总结"
git push origin main

# 工作结束
```

---

## 📌 提交说明的规范

### 推荐格式

```bash
# 新增功能
git commit -m "新增：功能描述"

# 修复 bug
git commit -m "修复：bug 描述"

# 优化改进
git commit -m "优化：改进内容"

# 更新文档
git commit -m "文档：更新内容"

# 代码重构
git commit -m "重构：重构说明"
```

---

## 🆘 常见问题处理

### Push 被拒绝

```bash
# 先拉取远程内容
git pull origin main

# 重新推送
git push origin main
```

### 需要认证

```bash
# 使用 SSH（推荐）
git remote set-url origin git@github.com:aliezbao/miniblog.git

# 或使用 HTTPS token
git remote set-url origin https://你的token@github.com/aliezbao/miniblog.git
```

### 查看谁修改了文件

```bash
# 查看每行代码的作者
git blame 文件名

# 查看某个作者的提交
git log --author="作者名"
```

### 比较两个版本的差异

```bash
# 查看本地与远程的差异
git diff origin/main

# 查看两个提交之间的差异
git diff 提交ID1 提交ID2
```

---

## 📊 分支操作（进阶）

```bash
# 查看所有分支
git branch -a

# 创建新分支
git branch 分支名

# 切换分支
git checkout 分支名

# 创建并切换分支
git checkout -b 新分支名

# 删除本地分支
git branch -d 分支名

# 删除远程分支
git push origin --delete 分支名

# 合并分支
git merge 分支名
```

---

## 🎓 学习资源

| 指令 | 说明 |
|------|------|
| `git help` | 显示帮助信息 |
| `git help 指令名` | 查看特定指令的详细帮助 |
| `git status` | 最常用，随时查看状态 |
| `git log --graph --oneline --all` | 可视化查看提交历史 |

---

